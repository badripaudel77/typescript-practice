"use strict";
exports.__esModule = true;
var citiesInNepal = ['Kathmandu', 'Pokhara', 'Baglung', 'Bharatpur', 'Biratnagar'];
var index = 0;
while (index < citiesInNepal.length) {
    console.log(citiesInNepal[index]);
    index++;
    // if(index === 2) {
    //     break; // or continue
    // }
}
