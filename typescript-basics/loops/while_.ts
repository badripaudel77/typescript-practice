export {}

let citiesInNepal = ['Kathmandu', 'Pokhara', 'Baglung', 'Bharatpur', 'Biratnagar'];

let index : number = 0;
while(index<citiesInNepal.length) {
    console.log(citiesInNepal[index]);
    index ++;

    // if(index === 2) {
    //     break; // or continue
    // }
}